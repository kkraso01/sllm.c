\section{Related Work}
Existing memory approaches span recurrent activation memory, memory tokens, and external retrieval stores, while test-time adaptation techniques often rely on parameter or prompt updates.

ALC focuses on explicit in-model mutable state and runtime persistence semantics. Relative to prior lines of work, this paper contributes mechanism-oriented evidence in a controlled delayed-recall setting rather than broad open-domain claims.
