\section{Results}
\subsection{Primary evidence: SessionKV-DR}
\begin{table}[h]
\centering
\begin{tabular}{llll}
\toprule
\input{tables/sessionkv_main.tex}
\bottomrule
\end{tabular}
\caption{SessionKV-DR delayed recall (mean $\pm$ std, 95\% CI, n=5).}
\end{table}

\begin{table}[h]
\centering
\begin{tabular}{llll}
\toprule
\input{tables/sessionkv_overwrite.tex}
\bottomrule
\end{tabular}
\caption{SessionKV-DR overwrite and persistence metrics (mean $\pm$ std, 95\% CI, n=5 where available).}
\end{table}

\begin{table}[h]
\centering
\begin{tabular}{llll}
\toprule
\input{tables/sessionkv_capacity.tex}
\bottomrule
\end{tabular}
\caption{SessionKV-DR capacity/interference sweep.}
\end{table}

SessionKV-DR is designed so success depends on inference-time session memory operations rather than generic QA priors. We therefore treat these tables as the primary evidence for ALC's mechanism claim. We report all three variants (baseline, no-write, full ALC) for each sub-evaluation and retain negative or mixed outcomes when no-write is competitive.

\subsection{Supporting evidence: TinyQA}
\begin{table}[h]
\centering
\begin{tabular}{llll}
\toprule
\input{tables/tinyqa_results.tex}
\bottomrule
\end{tabular}
\caption{TinyQA delayed QA accuracy (supporting evidence only).}
\end{table}

TinyQA remains useful as a semi-realistic check, but it is not used as the main claim test for structured write-retain-retrieve behavior.

\subsection{Additional diagnostics}
Main-package summaries, stability, trainability, ablations, and tiny efficiency checks are retained for reproducibility and context.
